/**
 * Kept as a server-side path so existing imports do not move. The definition is
 * in `@perfectman/shared` because the web stage renders private motives too and
 * must not import this package.
 */

export { ENGINE_MOTIVE_PREFIXES, isEngineAuthoredMotive } from "@perfectman/shared";
